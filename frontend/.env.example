# Copie para .env (frontend). Em dev, a rota /api é redirecionada para o backend
# via proxy do Vite. Em produção, use a URL completa do backend ou reescreva /api
# (ver docs/DEPLOY.md).
VITE_API_URL=/api